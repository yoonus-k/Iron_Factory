# 🚀 بطاقة المرجعية السريعة - أذن التسليم المبسطة

## ⚡ المعلومات الأساسية

| المعلومة | التفاصيل |
|---------|---------|
| **الملف الرئيسي** | `create.blade.php` |
| **الموقع** | `Modules/Manufacturing/resources/views/warehouses/delivery-notes/` |
| **عدد الأسطر** | 392 سطر |
| **التاريخ** | 23 نوفمبر 2025 |
| **الحالة** | ✅ جاهز للاستخدام |

---

## 📱 الحقول الأساسية

### للأذن الواردة (Incoming)
```
[ ] نوع الأذن (مطلوب)
[ ] التاريخ (مطلوب)
[ ] المورد (مطلوب)
[ ] المستودع (مطلوب)
[ ] الوزن بالكيلو (مطلوب)
```

### للأذن الصادرة (Outgoing)
```
[ ] نوع الأذن (مطلوب)
[ ] التاريخ (مطلوب)
[ ] المستودع المصدر (مطلوب)
[ ] المادة (مطلوب) ← تُحدث ديناميكياً
[ ] الكمية (مطلوب)
```

---

## 🔧 المتطلبات التقنية

### PHP
```
<?php
// في الـ Controller يجب أن يتوفر:
$suppliers      // قائمة الموردين
$warehouses     // قائمة المستودعات
$materialDetails // تفاصيل المواد المتاحة
```

### JavaScript
```javascript
// البيانات المتوقعة:
[
    {
        id: 1,
        material_id: 101,
        warehouse_id: 1,
        material_name: "اسم المادة",
        quantity: 500,
        unit_name: "كيلو"
    }
]
```

### CSS
```
// الفئات المستخدمة:
.form-section {}
.form-group {}
.form-input {}
.btn-submit {}
.btn-cancel {}
```

---

## 🎯 الحالات الخاصة

### عندما يختار "واردة":
```javascript
✅ إظهار: قسم الواردة
❌ إخفاء: قسم الصادرة
✅ إلزام: supplier_id, warehouse_id, invoice_weight
```

### عندما يختار "صادرة":
```javascript
❌ إخفاء: قسم الواردة
✅ إظهار: قسم الصادرة
✅ إلزام: warehouse_from_id, material_detail_id, delivery_quantity
```

### عند تغيير المستودع المصدر:
```javascript
✅ تحديث: قائمة المواد (إن وجدت)
✅ تفريغ: حقل المادة
✅ إعادة: عرض الكمية المتاحة
```

---

## ⚠️ رسائل الخطأ

| الرسالة | السبب | الحل |
|--------|------|------|
| "الرجاء اختيار نوع الأذن" | لم يتم اختيار النوع | اختر واردة أو صادرة |
| "الرجاء اختيار المورد" | لم يتم اختيار المورد | اختر من القائمة |
| "الرجاء اختيار المستودع" | لم يتم اختيار المستودع | اختر من القائمة |
| "الرجاء اختيار المادة" | لم يتم اختيار المادة | اختر من القائمة المحدثة |
| "الرجاء إدخال كمية صحيحة" | الكمية فارغة أو صفر | أدخل قيمة > 0 |

---

## 🔐 الحماية والتحقق

```javascript
// منع الإرسال المتكرر:
isSubmitting = false;
if (!isSubmitting) {
    isSubmitting = true;
    submitBtn.disabled = true;
}

// التحقق من الحقول:
if (!value) {
    e.preventDefault();
    return false;
}

// CSRF Token:
@csrf  // في الـ Blade
```

---

## 📊 الدوال الرئيسية

### `toggleSections()`
```javascript
// تعديل الأقسام حسب النوع المختار
// تحديث الحقول المطلوبة
```

### `updateMaterials()`
```javascript
// تحديث قائمة المواد حسب المستودع
// تفريغ الحقول السابقة
```

### معالج `change` للمادة
```javascript
// عرض الكمية المتاحة
// تحديث الحد الأقصى للكمية
```

---

## 💾 حفظ البيانات

```php
// في Controller:
$deliveryNote = DeliveryNote::create([
    'type' => $request->type,
    'delivery_date' => $request->delivery_date,
    'supplier_id' => $request->supplier_id,  // للواردة
    'warehouse_id' => $request->warehouse_id, // للواردة
    'warehouse_from_id' => $request->warehouse_from_id, // للصادرة
    'material_detail_id' => $request->material_detail_id, // للصادرة
    'delivery_quantity' => $request->delivery_quantity, // للصادرة
    'invoice_weight' => $request->invoice_weight, // للواردة
]);
```

---

## 🧪 الاختبار

### اختبار الأذن الواردة:
```
1. اختر "واردة"
2. أدخل: تاريخ، مورد، مستودع، وزن
3. اضغط حفظ
4. تحقق: ظهرت الأذن في القائمة
```

### اختبار الأذن الصادرة:
```
1. اختر "صادرة"
2. أدخل: تاريخ، مستودع، مادة، كمية
3. تحقق: الكمية المتاحة تظهر تلقائياً
4. اضغط حفظ
5. تحقق: ظهرت الأذن في القائمة
```

### حالات اختبار الأخطاء:
```
□ ترك حقل مطلوب فارغ
□ إدخال كمية سالبة
□ إدخال كمية أكبر من المتاح
□ الضغط السريع على الزر مرتين
□ تغيير المستودع بعد اختيار المادة
```

---

## 📁 الملفات المرتبطة

```
✅ create.blade.php          (النموذج الرئيسي)
✅ index.blade.php           (قائمة الأذون)
✅ DeliveryNoteController.php (معالج الطلبات)
✅ DeliveryNote.php          (الموديل)
✅ migration file            (قاعدة البيانات)
```

---

## 🎨 الألوان المستخدمة

```css
أخضر (#27ae60):     الواردة والنجاح
أزرق (#3498db):     البيانات الأساسية
أحمر (#e74c3c):     الأخطاء والحذف
رمادي (#95a5a6):    الأزرار المعطلة
أبيض (#ffffff):     الخلفية الأساسية
```

---

## 🚀 الخطوات السريعة للمطورين

### إضافة حقل جديد:
```blade
<!-- 1. أضف في النموذج -->
<div class="form-group">
    <label>الحقل الجديد</label>
    <input name="new_field">
</div>

<!-- 2. أضف في الـ Controller -->
'new_field' => 'required|string',

<!-- 3. أضف في Migration -->
$table->string('new_field');
```

### تصحيح خطأ:
```javascript
// 1. افتح الكونسول (F12)
console.log('Debug info:', variable);

// 2. تتبع الخطأ في الكود
// 3. أضف التعليق في الملف
// 4. اختبر التغيير
```

---

## 📞 معلومات التواصل السريعة

```
المشكلة:      الحل:
────────────  ────────────
خطأ الحقول   → تحقق من Required
الكمية:       → تحقق من Max value
المادة لا     → تحقق من Warehouse
تظهر:         Filter
```

---

## ✅ قائمة فحص سريعة

- [ ] النموذج يحمّل دون أخطاء
- [ ] الواردة تعرض الحقول الصحيحة
- [ ] الصادرة تعرض الحقول الصحيحة
- [ ] المواد تتحدث ديناميكياً
- [ ] الكمية المتاحة تظهر صحيحة
- [ ] الأخطاء تُعرض واضحة
- [ ] الزر لا ينقر مرتين
- [ ] البيانات تُحفظ في قاعدة البيانات

---

## 🎊 ملخص النقاط الرئيسية

✅ **بسيط**: 5 حقول فقط
✅ **سريع**: يحمّل في أقل من ثانية
✅ **آمن**: حماية CSRF + تحقق شامل
✅ **ذكي**: تحديث ديناميكي للبيانات
✅ **سهل**: واجهة بديهية واضحة
✅ **موثق**: توثيق شامل متوفر

---

**آخر تحديث:** 23 نوفمبر 2025
**الإصدار:** 1.0
**الحالة:** ✅ جاهز
