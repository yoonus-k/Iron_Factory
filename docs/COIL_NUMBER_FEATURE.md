# 🎯 نظام رقم الكويل (Coil Number)

## 📋 نظرة عامة

تم إضافة حقل **رقم الكويل (Coil Number)** لتتبع المواد الخام بشكل أفضل. الكويل هو رقم اللفة/الطرد من المورد.

---

## 🔧 التغييرات التقنية

### 1️⃣ قاعدة البيانات
```sql
-- جدول: material_batches
ALTER TABLE material_batches 
ADD COLUMN coil_number VARCHAR(100) NULL 
AFTER batch_code;

CREATE INDEX idx_coil_number ON material_batches(coil_number);
```

**الحقول:**
- `coil_number`: رقم الكويل من المورد (اختياري)
- يوجد index للبحث السريع

---

### 2️⃣ النماذج (Models)

**MaterialBatch.php:**
```php
protected $fillable = [
    'material_id',
    'unit_id',
    'batch_code',
    'coil_number', // ✅ رقم الكويل
    'latest_production_barcode',
    // ...
];
```

---

### 3️⃣ Controller

**WarehouseRegistrationController.php:**
```php
// Validation
'coil_number' => 'nullable|string|max:100'

// إنشاء الدفعة
MaterialBatch::create([
    'batch_code' => $batchCode,
    'coil_number' => $validated['coil_number'] ?? null,
    // ...
]);
```

---

### 4️⃣ واجهة المستخدم

**صفحة التسجيل (create.blade.php):**
```html
<input type="text" name="coil_number" 
       placeholder="مثال: C-2025-001 أو رقم الكويل من المورد">
```

**صفحة عرض الحركة (show.blade.php):**
- يظهر رقم الكويل في المودال عند عرض تفاصيل الحركة
- يظهر في بطاقة منفصلة داخل قسم معلومات الباركود

---

## 💡 كيفية الاستخدام

### 📥 عند استلام البضاعة:

1. افتح صفحة تسجيل الشحنة
2. املأ البيانات المطلوبة:
   - **المادة**: حديد خام
   - **الوزن**: 2000 كجم
   - **رقم الكويل**: `C-2025-001` ← اختياري
3. احفظ التسجيل

---

### 🔍 عند عرض الحركة:

في صفحة المستودع → جدول الحركات → اضغط "عرض":
```
📦 معلومات الباركود
├─ الباركود: RW-2025-051
├─ رقم الكويل: C-2025-001 ✅
├─ الكمية الأولية: 2000 كجم
└─ الكمية المتاحة: 2000 كجم
```

---

## 🎨 التصميم

### بطاقة رقم الكويل في المودال:
```css
background: rgba(255,255,255,0.15)
backdrop-filter: blur(10px)
border: 1px solid rgba(255,255,255,0.2)
padding: 12px
border-radius: 6px
```

**الألوان:**
- خلفية شفافة مع Blur effect
- نص أبيض (ضمن بطاقة الباركود البنفسجية/الخضراء)

---

## 📊 مثال عملي

### السيناريو:
المصنع يستلم **3 كويلات حديد** من المورد:

| الكويل | الوزن | الباركود | رقم الكويل |
|--------|-------|-----------|------------|
| 1 | 2 طن | RW-2025-051 | C-2025-001 |
| 2 | 1.8 طن | RW-2025-052 | C-2025-002 |
| 3 | 2.2 طن | RW-2025-053 | C-2025-003 |

**الفائدة:**
- يمكن تتبع كل كويل من المورد
- إذا كان هناك مشكلة في الجودة، يمكن معرفة رقم الكويل بسرعة
- يساعد في التواصل مع المورد

---

## ✅ الميزات

1. **اختياري**: غير مطلوب للتسجيل
2. **فريد لكل دفعة**: كل دفعة لها رقم كويل خاص
3. **قابل للبحث**: يوجد index للبحث السريع
4. **يظهر في التقارير**: متوفر في API والمودال

---

## 🔍 API Response

```json
{
  "movement": {
    "batch_code": "RW-2025-051",
    "coil_number": "C-2025-001",
    "material_name": "حديد خام",
    "quantity": "2,000.00"
  }
}
```

---

## 📝 ملاحظات مهمة

### الفرق بين الأرقام:

| النوع | الوصف | مثال | من أين؟ |
|-------|-------|------|---------|
| **Batch Code** | باركود النظام | RW-2025-051 | يُنشأ تلقائياً |
| **Coil Number** | رقم الكويل | C-2025-001 | من المورد |
| **Production Barcode** | باركود الإنتاج | PR-2025-009 | عند النقل للإنتاج |

---

## 🎯 الخلاصة

✅ **ما تم إضافته:**
1. حقل `coil_number` في قاعدة البيانات
2. حقل إدخال في صفحة التسجيل
3. عرض رقم الكويل في المودال
4. دعم API للحقل الجديد
5. Index للبحث السريع

✅ **الاستخدام:**
- اختياري (nullable)
- يظهر فقط إذا تم إدخاله
- يساعد في تتبع المواد من المورد

✅ **التوافق:**
- لا يؤثر على البيانات القديمة
- يعمل مع نظام الباركود الموجود
- متوافق مع نظام التتبع
