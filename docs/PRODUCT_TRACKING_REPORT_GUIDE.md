# تقرير التتبع الشامل للمنتجات 📊

## نظرة عامة
تقرير متقدم يتتبع جميع المنتجات من بداية الإنتاج حتى النهاية، مع تحليل تفصيلي للإنتاج والهدر والكفاءة.

## المميزات الرئيسية

### 1️⃣ الإحصائيات الملخصة
- إجمالي السجلات والعمليات
- إجمالي الإنتاج بالكيلوجرام
- إجمالي الهدر والنسبة المئوية
- متوسط الهدر لكل عملية
- عدد الباركودات والعمال والمراحل

### 2️⃣ الرسوم البيانية
- **رسم بياني الإنتاج اليومي**: يعرض الإنتاج والهدر لكل يوم
- **توزيع الإنتاج حسب المرحلة**: دائري يوضح نسبة كل مرحلة

### 3️⃣ التفاصيل حسب المرحلة
- عدد العمليات لكل مرحلة
- الإنتاج والهدر بالكيلوجرام
- نسبة الهدر والكفاءة

### 4️⃣ أفضل العمال أداءً
- ترتيب العمال حسب الإنتاج
- عدد العمليات والهدر والكفاءة
- التكلفة الإجمالية

### 5️⃣ تحليل الهدر
- الهدر الكلي لكل مرحلة
- متوسط وأقصى وأدنى نسب هدر
- مستويات الخطورة (حرج/تحذير/عادي)

### 6️⃣ المنتجات الأكثر متابعة
- أكثر 5 منتجات تتبعاً
- عدد خطوات التتبع
- المراحل والعمال المشاركين
- الحالة الحالية

### 7️⃣ جميع السجلات
- جدول مفصل لجميع العمليات
- مع إمكانية الترقيم التلقائي

## الفلاتر والخيارات

### 1. النطاق الزمني
- اختيار من التاريخ إلى التاريخ
- بحث ديناميكي

### 2. المرحلة
- جميع المراحل أو مرحلة محددة
- يتم جلب المراحل تلقائياً من قاعدة البيانات

### 3. الحالة
- جميع الحالات
- قيد المعالجة
- مكتمل
- مرفوض

### 4. العامل
- اختياري لعرض إحصائيات عامل محدد

## الرابط
```
/manufacturing/reports/product-tracking
```

## المسارات (Routes)

### عرض التقرير
```php
Route::get('reports/product-tracking', [ProductTrackingReportController::class, 'index'])
    ->name('manufacturing.reports.product-tracking');
```

## الصلاحيات المطلوبة
```
REPORTS_PRODUCT_TRACKING
```

## الملفات المستخدمة

### 1. المتحكم
- `ProductTrackingReportController.php`

### 2. النموذج
- `product-tracking-report.blade.php`

### 3. النموذج المستخدم
- `ProductTracking` (App\Models\ProductTracking)

## البيانات المعروضة

### من جدول `product_tracking`
- `barcode` - رقم الباركود
- `stage` - المرحلة الإنتاجية
- `action` - نوع الإجراء
- `input_weight` - وزن الإدخال
- `output_weight` - وزن الإخراج
- `waste_amount` - كمية الهدر
- `waste_percentage` - نسبة الهدر
- `cost` - التكلفة
- `worker_id` - معرف العامل
- `created_at` - وقت التسجيل

## الوظائف الرئيسية

### 1. `getTrackingRecords()`
جلب سجلات التتبع مع الفلاتر والترقيم

### 2. `getSummaryStatistics()`
حساب الإحصائيات الملخصة

### 3. `getProductionByStage()`
توزيع الإنتاج حسب المرحلة

### 4. `getTopPerformingWorkers()`
ترتيب أفضل العمال

### 5. `getDailyProductionTrend()`
الاتجاه اليومي للإنتاج

### 6. `getWasteAnalysis()`
تحليل الهدر المفصل

### 7. `getStagePerformanceComparison()`
مقارنة أداء المراحل

### 8. `getTopTrackedBarcodes()`
أكثر الباركودات تتبعاً

## الاستخدام

### العرض العادي
1. انتقل إلى **التقارير > تقرير التتبع الشامل**
2. اختر النطاق الزمني والفلاتر (اختياري)
3. اضغط **بحث** للتحديث
4. يمكن **طباعة** التقرير

### الفلاتر
```
معلومات المرح
```

## ملاحظات تقنية

- التقرير يستخدم **pagination** للسجلات (20 سجل/صفحة)
- الرسوم البيانية تستخدم **Chart.js**
- الجدول يدعم الترتيب والبحث
- مصمم بشكل **متجاوب** (Responsive)
- يدعم **الطباعة** المباشرة
- يعمل مع قاعدة البيانات الحقيقية بدون تزييف البيانات

## التكامل مع النظام

### القائمة الجانبية
تم إضافة الرابط تحت:
**التقارير > تقارير التتبع > تقرير التتبع الشامل**

### الصلاحيات
يتطلب صلاحية `REPORTS_PRODUCT_TRACKING`

## التحديثات المستقبلية

- [ ] إضافة تصدير PDF
- [ ] إضافة تصدير Excel
- [ ] إضافة رسوم بيانية متقدمة
- [ ] إضافة مقارنة فترات زمنية
- [ ] إضافة تنبيهات تلقائية

---

**تاريخ الإنشاء:** 2025-12-11
**الإصدار:** 1.0
