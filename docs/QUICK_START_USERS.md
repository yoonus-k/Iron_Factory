# نظام إدارة المستخدمين - دليل الاستخدام السريع

## 🎯 نقاط الدخول الرئيسية

```
┌─────────────────────────────────────────────────────────┐
│          نظام إدارة المستخدمين الكامل                    │
├─────────────────────────────────────────────────────────┤
│                                                           │
│  📄 /users  ──────────────────────────────────────────   │
│    └─ قائمة المستخدمين                                  │
│       ├─ بحث متقدم                                      │
│       ├─ تصفية حسب الدور                                │
│       ├─ تصفية حسب الحالة                               │
│       └─ ترقيم الصفحات (20 مستخدم/صفحة)               │
│                                                           │
│  ➕ /users/create  ──────────────────────────────────    │
│    └─ إضافة مستخدم جديد                                │
│       ├─ اسم كامل                                       │
│       ├─ بريد إلكتروني                                  │
│       ├─ اسم مستخدم                                     │
│       ├─ كلمة مرور                                      │
│       ├─ اختيار الدور                                   │
│       ├─ فترة زمنية                                     │
│       └─ تفعيل الحساب                                   │
│                                                           │
│  ✏️ /users/{id}/edit  ──────────────────────────────    │
│    └─ تعديل بيانات المستخدم                            │
│       ├─ جميع الحقول قابلة للتعديل                    │
│       ├─ كلمة المرور اختيارية                          │
│       └─ معلومات النظام (للاطلاع فقط)                 │
│                                                           │
│  👁️ /users/{id}  ──────────────────────────────────     │
│    └─ عرض تفاصيل المستخدم الكاملة                      │
│       ├─ البيانات الأساسية                             │
│       ├─ الدور والصلاحيات                              │
│       ├─ سجل العمليات الأخيرة                          │
│       └─ معلومات النظام                                │
│                                                           │
└─────────────────────────────────────────────────────────┘
```

---

## 📊 العمليات الممكنة

### 1️⃣ عرض قائمة المستخدمين
```
GET /users
```
**ما يتم عرضه:**
- جدول بجميع المستخدمين
- معلومات: ID, الاسم, اسم المستخدم, البريد, الدور, الفترة, الحالة
- أزرار الإجراءات لكل مستخدم

**خيارات البحث:**
- 🔍 البحث بالاسم أو البريد أو اسم المستخدم
- 🏷️ تصفية حسب الدور
- 🔄 تصفية حسب الحالة (نشط/غير نشط)

---

### 2️⃣ إضافة مستخدم جديد
```
GET /users/create   # عرض النموذج
POST /users         # حفظ البيانات
```

**الحقول المطلوبة:**
```
✓ الاسم الكامل          (text) 
✓ اسم المستخدم         (text - فريد)
✓ البريد الإلكتروني     (email - فريد)
✓ كلمة المرور          (password - 8+ أحرف)
✓ تأكيد كلمة المرور    (password)
✓ الدور                (select - من الأدوار النشطة)

⚪ الفترة الزمنية       (text - اختياري)
⚪ تفعيل الحساب        (checkbox - مختار افتراضياً)
```

**رسالة النجاح:**
```
✅ تم إنشاء المستخدم بنجاح
```

---

### 3️⃣ تعديل بيانات المستخدم
```
GET /users/{id}/edit    # عرض النموذج
PUT /users/{id}         # تحديث البيانات
```

**الحقول:**
- جميع الحقول من الإنشاء (ماعدا اسم المستخدم والبريد)
- كلمة المرور: اختيارية (اتركها فارغة للحفاظ عليها)
- معلومات الإنشاء والتحديث (للاطلاع فقط)

**رسالة النجاح:**
```
✅ تم تحديث المستخدم بنجاح
```

---

### 4️⃣ عرض التفاصيل الكاملة
```
GET /users/{id}
```

**أقسام الصفحة:**

1. **البيانات الأساسية:**
   - الاسم والبريد واسم المستخدم
   - الفترة الزمنية وحالة الحساب

2. **الدور والصلاحيات:**
   - الدور الأساسي
   - وصف الدور ومستواه
   - الصلاحيات الاستثنائية

3. **سجل العمليات (آخر 10):**
   - التاريخ والوقت
   - نوع العملية
   - المستخدم المسؤول

4. **معلومات النظام:**
   - تاريخ الإنشاء
   - آخر تحديث

---

### 5️⃣ حذف المستخدم
```
DELETE /users/{id}
```

**التحقق:**
- ✓ لا يمكن حذف حسابك الخاص
- ✓ تأكيد الحذف من المستخدم
- ✓ حذف جميع الصلاحيات المرتبطة

**رسالة النجاح:**
```
✅ تم حذف المستخدم بنجاح
```

---

## 🔐 الصلاحيات المطلوبة

### على مستوى الوصول:
```
⚡ الدور المطلوب: ADMIN فقط
```

### على مستوى الإجراءات:
```
📖 عرض (Read)      - MANAGE_USERS:read
➕ إضافة (Create)   - MANAGE_USERS:create
✏️ تعديل (Update)   - MANAGE_USERS:update
🗑️ حذف (Delete)     - MANAGE_USERS:delete
```

---

## 🎨 رسائل النظام

### رسائل النجاح (🟢 أخضر)
```
✅ تم إنشاء المستخدم بنجاح
✅ تم تحديث المستخدم بنجاح
✅ تم حذف المستخدم بنجاح
✅ تم تفعيل المستخدم
✅ تم تعطيل المستخدم
```

### رسائل الخطأ (🔴 أحمر)
```
❌ لا يمكنك حذف حسابك الخاص
❌ لا يمكنك تعطيل حسابك الخاص
❌ حدث خطأ: [تفاصيل الخطأ]
```

### رسائل التحقق
```
⚠️ هذا البريد مستخدم بالفعل
⚠️ هذا اسم المستخدم مستخدم بالفعل
⚠️ كلمة المرور يجب أن تكون 8 أحرف على الأقل
```

---

## 📱 الواجهة والتصميم

### المميزات:
✅ تصميم استجابي (Responsive)
✅ محاذاة من اليمين (RTL)
✅ ألوان واضحة للحالات المختلفة
✅ أيقونات Font Awesome
✅ جداول منسقة

### الألوان المستخدمة:
- 🔵 **أزرق** - الإجراءات الأساسية
- 🟢 **أخضر** - الحفظ والنجاح
- 🔴 **أحمر** - الحذف والخطأ
- ⚫ **رمادي** - المعلومات الإضافية

---

## 🚀 أمثلة عملية

### مثال 1: إضافة موظف جديد
```
1. اذهب إلى /users
2. اضغط "إضافة مستخدم جديد"
3. ملء البيانات:
   - الاسم: أحمد محمد
   - اسم المستخدم: ahmed.m
   - البريد: ahmed@example.com
   - كلمة المرور: SecurePass123
   - الدور: موظف
   - الفترة: صباحي
4. اضغط "إضافة المستخدم"
5. ستشاهد الرسالة: "تم إنشاء المستخدم بنجاح"
```

### مثال 2: تعديل فترة العمل
```
1. اذهب إلى /users
2. ابحث عن المستخدم
3. اضغط على أيقونة التعديل (✏️)
4. غيّر الفترة من "صباحي" إلى "مسائي"
5. اضغط "تحديث المستخدم"
```

### مثال 3: عرض تفاصيل المستخدم
```
1. اذهب إلى /users
2. اضغط على أيقونة العرض (👁️)
3. سترى جميع المعلومات:
   - البيانات الأساسية
   - الدور والصلاحيات
   - سجل العمليات
   - معلومات النظام
```

---

## 📋 ملخص المتطلبات

| المتطلب | التفاصيل |
|--------|---------|
| **الدور** | Admin فقط |
| **الصلاحية** | MANAGE_USERS |
| **المتصفح** | أي متصفح حديث |
| **الاتصال** | بدون اتصال إنترنت (محلي) |

---

## 🎓 نصائح مهمة

1. **البحث السريع**: استخدم البحث للعثور على مستخدم بسرعة
2. **التصفية**: ادمج البحث والتصفية للنتائج الدقيقة
3. **كلمات المرور**: تأكد من كلمات مرور قوية (8+ أحرف)
4. **النسخ الاحتياطي**: الفريدية مضمونة (بريد واسم مستخدم)
5. **الحماية**: لا يمكن حذف حسابك الخاص لأسباب أمنية

---

**نسخة المستند**: 1.0
**آخر تحديث**: 24 نوفمبر 2025
