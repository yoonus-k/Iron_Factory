# نظام إدارة المستخدمين 👥

## الميزات الرئيسية

تم إنشاء نظام إدارة مستخدمين كامل يتضمن:

### 1. **صفحة Index (قائمة المستخدمين)**
- عرض جميع المستخدمين مع معلومات كاملة
- **البحث**: البحث حسب الاسم أو البريد الإلكتروني أو اسم المستخدم
- **التصفية**: 
  - حسب الدور (Role)
  - حسب حالة الحساب (نشط / غير نشط)
- **الترقيم**: صفحات مع 20 مستخدم لكل صفحة
- **الإجراءات**: عرض، تعديل، حذف

**المسار**: `/users`

### 2. **صفحة Create (إضافة مستخدم جديد)**
يحتوي النموذج على الحقول التالية:
- ✅ **الاسم الكامل** (مطلوب)
- ✅ **اسم المستخدم** (مطلوب، فريد)
- ✅ **البريد الإلكتروني** (مطلوب، فريد)
- ✅ **كلمة المرور** (مطلوب، 8 أحرف على الأقل)
- ✅ **تأكيد كلمة المرور** (مطلوب)
- ✅ **الدور** (مطلوب) - اختيار من الأدوار النشطة
- ✅ **الفترة الزمنية** (اختياري) - صباحي، مسائي، إلخ
- ✅ **تفعيل الحساب** (checkbox) - تفعيل تلقائي عند الإنشاء

**المسار**: `/users/create`

### 3. **صفحة Edit (تعديل المستخدم)**
نفس الحقول مع بعض التعديلات:
- كلمة المرور **اختيارية** (اتركها فارغة للعدم تغييرها)
- عرض معلومات النظام (رقم المستخدم، تاريخ الإنشاء، آخر تحديث)
- تحديث جميع البيانات

**المسار**: `/users/{id}/edit`

### 4. **صفحة Show (عرض التفاصيل)**
عرض شامل لمعلومات المستخدم:

#### بيانات المستخدم الأساسية:
- الاسم الكامل
- اسم المستخدم
- البريد الإلكتروني
- الفترة الزمنية
- حالة الحساب (نشط/غير نشط)
- رقم المستخدم

#### الدور والصلاحيات:
- الدور الأساسي المعين
- وصف الدور
- مستوى الدور
- الصلاحيات الاستثنائية (إن وجدت)

#### سجل العمليات:
- آخر 10 عمليات تمت على نظام المستخدم
- التاريخ والوقت
- نوع العملية
- المستخدم الذي أجراها

#### معلومات النظام:
- تاريخ الإنشاء
- آخر تحديث

**المسار**: `/users/{id}`

---

## متطلبات الصلاحيات

جميع الصفحات محمية بـ **دور Admin** فقط:
```php
Route::middleware(['role:ADMIN'])->group(function () {
    Route::resource('users', UserController::class);
});
```

### الصلاحيات المفصلة:
- **Read (عرض)**: `MANAGE_USERS` + `read`
- **Create (إضافة)**: `MANAGE_USERS` + `create`
- **Update (تعديل)**: `MANAGE_USERS` + `update`
- **Delete (حذف)**: `MANAGE_USERS` + `delete`

---

## الميزات الأمنية

✅ **حماية من الحذف الذاتي**: لا يمكن حذف حسابك الخاص
✅ **تشفير كلمات المرور**: تستخدم `bcrypt`
✅ **فريدية البيانات**: البريد واسم المستخدم فريدان
✅ **معالجة الأخطاء**: رسائل واضحة للمستخدم
✅ **Transactions**: عمليات آمنة مع rollback عند الخطأ

---

## المسارات (Routes)

```php
GET    /users              # قائمة المستخدمين (index)
POST   /users              # إنشاء مستخدم (store)
GET    /users/create       # صفحة الإضافة (create)
GET    /users/{id}         # عرض التفاصيل (show)
GET    /users/{id}/edit    # صفحة التعديل (edit)
PUT    /users/{id}         # تحديث البيانات (update)
DELETE /users/{id}         # حذف المستخدم (destroy)
```

---

## ملفات المشروع المضافة

### Controllers:
- `app/Http/Controllers/UserController.php`

### Views:
- `resources/views/users/index.blade.php` - قائمة المستخدمين
- `resources/views/users/create.blade.php` - نموذج الإضافة
- `resources/views/users/edit.blade.php` - نموذج التعديل
- `resources/views/users/show.blade.php` - عرض التفاصيل

### Routes:
- تم إضافة `Route::resource('users', UserController::class)` في `routes/web.php`

---

## أمثلة الاستخدام

### إضافة مستخدم جديد:
1. اذهب إلى `/users`
2. اضغط على "إضافة مستخدم جديد"
3. ملء جميع الحقول المطلوبة
4. اضغط "إضافة المستخدم"

### تعديل مستخدم:
1. اذهب إلى `/users`
2. اضغط على أيقونة التعديل (Edit)
3. عدّل البيانات المطلوبة
4. اضغط "تحديث المستخدم"

### حذف مستخدم:
1. اذهب إلى `/users`
2. اضغط على أيقونة الحذف (Delete)
3. أكد الحذف من رسالة التأكيد

---

## رسائل النظام

- ✅ "تم إنشاء المستخدم بنجاح"
- ✅ "تم تحديث المستخدم بنجاح"
- ✅ "تم حذف المستخدم بنجاح"
- ❌ "لا يمكنك حذف حسابك الخاص"
- ❌ "لا يمكنك تعطيل حسابك الخاص"

---

## ملاحظات مهمة

1. **كلمة المرور**: عند الإنشاء مطلوبة، وعند التعديل اختيارية
2. **الدور**: يجب اختيار دور من الأدوار النشطة فقط
3. **الفريدية**: البريد واسم المستخدم فريدان (لا تكرار)
4. **صلاحيات Admin**: فقط المسؤولون يستطيعون إدارة المستخدمين

---

**تاريخ الإنشاء**: 24 نوفمبر 2025
