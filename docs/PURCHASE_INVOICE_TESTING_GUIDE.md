# خطوات الاختبار - نظام المنتجات المتعددة في فواتير الشراء

## 🧪 اختبار إضافة فاتورة جديدة مع منتجات

### 1. الوصول إلى صفحة الإضافة
```
URL: http://localhost/manufacturing/purchase-invoices/create
```

### 2. ملء البيانات الأساسية
- رقم الفاتورة: `INV-TEST-001`
- تاريخ الفاتورة: `2025-11-18`
- اختر المورد من القائمة
- رقم مرجع الفاتورة: `REF-001` (اختياري)
- تاريخ الاستحقاق: `2025-12-18` (اختياري)

### 3. إضافة المنتج الأول
سيظهر منتج واحد تلقائيًا:

#### المنتج 1: حديد تسليح
1. **اختر مادة من القائمة** (أو اتركها فارغة للإدخال يدويًا)
2. **اسم المنتج**: `حديد تسليح 12 ملم`
3. **الوصف**: `حديد تسليح قطر 12 ملم جودة عالية`
4. **الكمية**: `100`
5. **الوحدة**: `طن`
6. **سعر الوحدة**: `2500`
7. **نسبة الضريبة**: `15`
8. **نسبة الخصم**: `5`
9. **ملاحظات**: `دفعة أولى`

**النتيجة المتوقعة**:
- المجموع الفرعي: 250,000
- الضريبة: 37,500
- الخصم: 12,500
- الإجمالي: 275,000

### 4. إضافة منتج ثانٍ
انقر على زر "إضافة منتج" 🟢

#### المنتج 2: إسمنت بورتلاند
1. **اسم المنتج**: `إسمنت بورتلاند`
2. **الوصف**: `إسمنت بورتلاند 50 كجم`
3. **الكمية**: `50`
4. **الوحدة**: `طن`
5. **سعر الوحدة**: `350`
6. **نسبة الضريبة**: `15`
7. **نسبة الخصم**: `0`
8. **ملاحظات**: `توصيل سريع`

**النتيجة المتوقعة**:
- المجموع الفرعي: 17,500
- الضريبة: 2,625
- الخصم: 0
- الإجمالي: 20,125

### 5. إضافة منتج ثالث
انقر على زر "إضافة منتج" 🟢 مرة أخرى

#### المنتج 3: طوب أحمر
1. **اسم المنتج**: `طوب أحمر`
2. **الكمية**: `10000`
3. **الوحدة**: `قطعة`
4. **سعر الوحدة**: `0.50`
5. **نسبة الضريبة**: `15`
6. **نسبة الخصم**: `10`

**النتيجة المتوقعة**:
- المجموع الفرعي: 5,000
- الضريبة: 750
- الخصم: 500
- الإجمالي: 5,250

### 6. مراقبة الملخص الإجمالي
يجب أن يظهر في أسفل قسم المنتجات:

| البند | القيمة |
|-------|--------|
| الإجمالي الفرعي | 272,500 |
| الضريبة | 40,875 |
| الخصم | 13,000 |
| **الإجمالي النهائي** | **300,375** |

### 7. اختيار العملة وشروط الدفع
- **العملة**: `SAR - ريال سعودي`
- **شروط الدفع**: `30 يوم`

### 8. اختيار الحالة
- **الحالة**: `مسودة` أو `في الانتظار`
- **النشاط**: ✅ الفاتورة نشطة

### 9. ملاحظات عامة (اختياري)
```
فاتورة شراء مواد بناء للمشروع رقم 2025-A
تم التفاوض على الأسعار مع المورد
```

### 10. الحفظ
انقر على زر "حفظ الفاتورة" 💾

---

## 🔄 اختبار تعديل فاتورة

### 1. الانتقال لصفحة العرض
بعد الحفظ، ستنتقل تلقائيًا لصفحة عرض الفاتورة

### 2. انقر على "تعديل"
انتقل لصفحة التعديل

### 3. تعديل منتج موجود
- غيّر كمية المنتج الأول من `100` إلى `120`
- لاحظ تحديث الحسابات تلقائيًا

### 4. حذف منتج
- انقر على زر "حذف" 🔴 بجانب المنتج الثالث
- لاحظ إعادة ترقيم المنتجات

### 5. إضافة منتج جديد
- انقر "إضافة منتج"
- أضف منتج رابع

### 6. حفظ التعديلات
انقر على "حفظ التغييرات"

---

## ✅ نقاط الاختبار الهامة

### اختبار الواجهة:
- [ ] ظهور حقول المنتج بشكل صحيح
- [ ] زر "إضافة منتج" يعمل
- [ ] زر "حذف" يعمل لكل منتج
- [ ] إعادة ترقيم المنتجات بعد الحذف
- [ ] قائمة المواد تظهر بشكل صحيح

### اختبار الحسابات:
- [ ] حساب المجموع الفرعي صحيح
- [ ] حساب الضريبة صحيح
- [ ] حساب الخصم صحيح
- [ ] حساب الإجمالي لكل منتج صحيح
- [ ] الملخص الإجمالي صحيح

### اختبار التفاعل:
- [ ] اختيار مادة من القائمة يملأ الحقول
- [ ] تغيير الكمية يحدّث الحسابات
- [ ] تغيير السعر يحدّث الحسابات
- [ ] تغيير الضريبة يحدّث الحسابات
- [ ] تغيير الخصم يحدّث الحسابات

### اختبار الـ Validation:
- [ ] لا يمكن الحفظ بدون منتج
- [ ] لا يمكن الحفظ بدون اسم منتج
- [ ] لا يمكن الحفظ بدون كمية
- [ ] لا يمكن الحفظ بدون سعر
- [ ] الكمية يجب أن تكون أكبر من صفر

### اختبار قاعدة البيانات:
```sql
-- عرض الفواتير
SELECT * FROM purchase_invoices ORDER BY id DESC LIMIT 1;

-- عرض منتجات الفاتورة الأخيرة
SELECT * FROM purchase_invoice_items 
WHERE purchase_invoice_id = (SELECT MAX(id) FROM purchase_invoices);

-- التحقق من الحسابات
SELECT 
    item_name,
    quantity,
    unit_price,
    subtotal,
    tax_rate,
    tax_amount,
    discount_rate,
    discount_amount,
    total
FROM purchase_invoice_items
WHERE purchase_invoice_id = [ID];
```

---

## 🐛 مشاكل محتملة وحلولها

### المشكلة 1: لا تظهر المواد في القائمة
**الحل**: تأكد من وجود مواد نشطة في جدول `materials`
```sql
SELECT * FROM materials WHERE is_active = 1;
```

### المشكلة 2: الحسابات غير صحيحة
**الحل**: تحقق من JavaScript console في المتصفح (F12)

### المشكلة 3: خطأ عند الحفظ
**الحل**: 
1. تحقق من Laravel logs: `storage/logs/laravel.log`
2. تأكد من وجود جدول `purchase_invoice_items`
```sql
SHOW TABLES LIKE 'purchase_invoice_items';
```

### المشكلة 4: الفاتورة محفوظة بدون منتجات
**الحل**: تحقق من:
1. اسم الحقول في الـ form
2. validation rules في الـ Controller
3. Database transaction في store method

---

## 📸 صور متوقعة للنتائج

### صفحة الإضافة:
```
┌─────────────────────────────────────────────┐
│  إضافة فاتورة شراء جديدة                    │
├─────────────────────────────────────────────┤
│  معلومات الفاتورة                          │
│  [رقم الفاتورة] [التاريخ] [المورد]         │
├─────────────────────────────────────────────┤
│  منتجات الفاتورة                           │
│  ┌───────────────────────────────────────┐  │
│  │ منتج #1              [حذف]           │  │
│  │ [المادة ▼] [اسم المنتج]              │  │
│  │ [الكمية] [الوحدة] [السعر]            │  │
│  │ [الضريبة] [الخصم] [الإجمالي: 0.00]  │  │
│  └───────────────────────────────────────┘  │
│  [+ إضافة منتج]                            │
│                                             │
│  ┌───────────────────────────────────────┐  │
│  │ الإجمالي الفرعي:        0.00         │  │
│  │ الضريبة:                0.00         │  │
│  │ الخصم:                  0.00         │  │
│  │ الإجمالي النهائي:       0.00         │  │
│  └───────────────────────────────────────┘  │
├─────────────────────────────────────────────┤
│  [💾 حفظ الفاتورة]  [❌ إلغاء]            │
└─────────────────────────────────────────────┘
```

---

## 🎯 معايير النجاح

✅ **الاختبار ناجح إذا**:
1. تم إنشاء فاتورة مع 3 منتجات على الأقل
2. جميع الحسابات صحيحة
3. تم حفظ البيانات في قاعدة البيانات
4. يمكن تعديل الفاتورة والمنتجات
5. يمكن حذف منتج وإضافة آخر
6. الواجهة سلسة وسريعة الاستجابة

---

**تاريخ الاختبار**: ___________  
**المختبِر**: ___________  
**النتيجة**: ⭕ نجح / ❌ فشل
